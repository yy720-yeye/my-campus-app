# Nixpacks configuration for Railway deployment
# Override default Node.js version and install command

[phases.setup]
nixPkgs = ["nodejs_23"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]